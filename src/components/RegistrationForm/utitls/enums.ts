export enum ValidationErrorMessages {
  required = 'Cannot be blank',
  letterOnly = 'This input is letter only.',
  numberOnly = 'This input is number only.',
  minNumber = 'Number cannot be below 0',
  maxLength = 'Address is too long.',
}
